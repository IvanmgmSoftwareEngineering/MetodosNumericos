%% Script1

% TITULO: Polinomio Lagrange e Interpolaci?n mediante Polinomio de Lagrange
% AUTOR: Ivan Martin GomeZ
%
% DESCRIPCION: Este Script1
%------------------------------------------------------
% Argumentos de Entrada al Programa (Modificables por el usuario)

% ARGUMENTO 1: Intervalo Cerrado, dado mediante dos valores enteros, Extremo Inferior
% del Intervalo (a) y Extremo Superior del Intervalo (b). [a,b]

extremo_Inferior_Intervalo = 0;
extremo_Superior_Intervalo = 2;

% ARGUMENTO 2: N?mero de puntos del Intervalo Cerrado que vamos a utilizar en la Interpolaci?n
%Del Intervalo Cerrado [a,b] nos quedamos con numPuntos (n) del Intervalo.
%[a=xo, x1, x2, ..., b=xn], expresado en la notacion de Matlab (las
%primeras posiciones de los vectores se indexan

numPuntos = 2;

%FIN Argumentos de Entrada al Programa
%-------------------------------------------------------

%asignacion=input('Indique el numero del Rutina que quiere Ejecutar: ')


%Llamo a una rutina.m desde este Script. La rutina.m para poder ser
%llamada desde este Script debe estar alojada en la misma caprpeta qeu el Script que la esta
%llamando
if asignacion==1
    rutina1
elseif asignacion==2
    rutina2
elseif asignacion~=1 || asignacion~=2
    x='ERROR'
end
%Llamo a una funcion.m desde este Script. La funcion.m para poder ser
%llamada desde este Script debe estar alo~jada en la misma caprpeta qeu el Script que la esta
%llamando
matriz_polinomio_Lagrange = zeros (
polinomioLagrange


